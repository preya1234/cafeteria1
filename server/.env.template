# MongoDB Atlas Connection String
# Get this from: https://cloud.mongodb.com -> Database -> Connect -> Connect your application
# Replace <password> with your actual database password
# Replace <dbname> with Cafeteria (or your database name)

MONGODB_URI=mongodb+srv://your_username:your_password@cluster0.2cwcywf.mongodb.net/Cafeteria?retryWrites=true&w=majority&appName=Cluster0

# Instructions:
# 1. Get connection string from MongoDB Atlas
# 2. Replace 'your_username' with your MongoDB Atlas database username
# 3. Replace 'your_password' with your MongoDB Atlas database password
# 4. If password has special characters, URL-encode them (@ becomes %40, # becomes %23, etc.)
# 5. Save this file and run: node seedProducts.js
